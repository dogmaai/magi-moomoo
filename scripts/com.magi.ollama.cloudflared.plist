<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  com.magi.ollama.cloudflared.plist — macOS LaunchAgent for the Ollama Cloudflare named tunnel.

  Install:
    cp scripts/com.magi.ollama.cloudflared.plist ~/Library/LaunchAgents/
    launchctl load ~/Library/LaunchAgents/com.magi.ollama.cloudflared.plist

  Uninstall:
    launchctl unload ~/Library/LaunchAgents/com.magi.ollama.cloudflared.plist
    rm ~/Library/LaunchAgents/com.magi.ollama.cloudflared.plist

  Prerequisites:
    - Run: bash scripts/setup-ollama-named-tunnel.sh <hostname> (one-time)
    - Update WorkingDirectory below to match your local repo path.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.magi.ollama.cloudflared</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>scripts/start-ollama-named-tunnel.sh</string>
    </array>

    <key>WorkingDirectory</key>
    <string>/Users/jun/magi-moomoo</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
    </dict>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
    </dict>

    <key>StandardOutPath</key>
    <string>/tmp/magi-ollama-cloudflared.stdout.log</string>

    <key>StandardErrorPath</key>
    <string>/tmp/magi-ollama-cloudflared.stderr.log</string>

    <key>ThrottleInterval</key>
    <integer>30</integer>
</dict>
</plist>
